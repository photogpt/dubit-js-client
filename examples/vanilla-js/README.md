# Vanilla JS + HTML live audio translation

This example demonstrates how dubit-js can be used to translate audio in realtime.

## How to run?

- Install dependencies:

  ```bash
  npm install
  ```

- Run `npm run dev` and visit <http://localhost:5173>
